Write the full test class for $ARGUMENTS following CLAUDE.md, then make
it pass. Apply the per-class workflow: read the target class, enumerate
every branch, write the test, run mvn -q -Dtest=<TestClass> test until
green, then mvn -q verify and fill any uncovered branch or line of the
target class. Finish with the report: branch count n/n, files read,
iterations, and any justified residue.
